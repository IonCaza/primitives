# Agentic AI Engine - Python dependencies
# Add these to your project's requirements.txt

# LangChain ecosystem
langchain>=1.2
langchain-community>=0.4
langchain-litellm>=0.6
langgraph>=1.1
langgraph-checkpoint-postgres>=3.0
langmem>=0.0.30

# LLM gateway
litellm>=1.83

# LangGraph memory infrastructure (psycopg, not asyncpg)
psycopg[binary]>=3.2
psycopg-pool>=3.2

# Encryption for API keys
cryptography>=44.0

# SSE streaming
sse-starlette>=2.0

# Already required by most FastAPI projects:
# fastapi, sqlalchemy[asyncio], asyncpg, pydantic, uvicorn, redis
